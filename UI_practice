/**** Start of imports. If edited, may not auto-convert in the playground. ****/
var table = ee.FeatureCollection("users/nguy3856/senegalAdminLevel2"),
    table3 = ee.FeatureCollection("users/nguy3856/senegalAdminLevel1");
/***** End of imports. If edited, may not auto-convert in the playground. *****/
// Make a button widget.
var button = ui.Button('Click me!');

// Set a callback function to run when the
// button is clicked.
button.onClick(function() {
  print('Hello, world!');
});

// Display the button in the console.
print(button);

// Set another callback function on the button.
button.onClick(function() {
  print('Oh, yeah!');
});

var senegalStates = ee.FeatureCollection(table3);
Map.addLayer(senegalStates, {}, 'states');
var senegalCounties = ee.FeatureCollection(table);
Map.addLayer(senegalCounties, {}, 'counties');
Map.centerObject(senegalCounties);